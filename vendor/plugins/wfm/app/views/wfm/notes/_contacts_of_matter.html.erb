<%unless contacts.blank?%>
  <%= select :note, :contact_id, contacts.collect {|u| [ u.full_name, u.id ]}, {:selected=>selected_contact_id,:prompt=>"Please Select"},:onchange =>"update_matter_select(this.value)"%>
<%else%>
  <%= select :note, :contact_id,[] %>
<%end%>