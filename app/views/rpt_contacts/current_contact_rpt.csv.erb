<%=@header_opts[:r_name]%>

<%-@total_data.each do |label,data|-%>
<%="#{(label == "" or !label) ? "None" : label}" + " (#{data.length} Records)"%>
<%=@table_headers.join(",")%>
<%-data.each do |contact|-%>
<%=contact.join(",")%>
<%-end-%>


<%-end-%>
