<div id="topheadContainer"><div class="title fl">Edit Task Type</div></div>
<div id="nameerror"  class="errorCont" style="display:none"></div>
<%  remote_form_for(:task_type,@task_type, :url => company_task_type_path(@company,@task_type),:loading=>"showLoadingGif();",:complete=>"hidewLoadingGif();",:html => { :method => :put}) do |f|%>
  <%= render :partial=>"company/form", :locals => {:f => f, :modelclass => "task_types",
    :select_modal_text => "Task Type", :modal_text => "Task Type",
    :all_modal_values => @company.task_types.collect{|task|[task.lvalue]}.uniq } %>
<%end%>
<br class="clear"/>