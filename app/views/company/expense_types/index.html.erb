<div class="gridDiv">
  <div class="titleHead"><%= @company.name.try(:titleize) %>'s <%=t(:text_s_time_expense_type)%></div>
  <div class="gryBox ">
    <div id="list">
      <%= render :partial =>"list" %>
    </div>
  </div>
</div>